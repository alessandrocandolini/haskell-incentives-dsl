name: Compile and run tests

on:
  push:
    branches: [ main, feature/** ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # enable manual execution

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  STACK_VERSION: "3.9.3"

jobs:
  stack:

    name: ${{ matrix.os }} / stack
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:

    - name: Checkout repo
      uses: actions/checkout@v6

    - name: Setup Haskell Stack
      id: setup
      uses: haskell-actions/setup@v2
      with:
        enable-stack: true
        stack-no-global: true
        stack-version: ${{ env.STACK_VERSION }}

    - name: Cache Stack programs
      id: stack-programs-cache
      uses: actions/cache@v5
      with:
        path: ${{ steps.setup.outputs.stack-root }}/programs
        key: ${{ runner.os }}-stack-programs-${{ env.STACK_VERSION }}-${{ hashFiles('stack.yaml', 'stack.yaml.lock') }}
        restore-keys: |
          ${{ runner.os }}-stack-programs-${{ env.STACK_VERSION }}-

    - name: Cache Stack dependencies
      id: stack-deps-cache
      uses: actions/cache@v5
      with:
        path: |
          ${{ steps.setup.outputs.stack-root }}/build-plan
          ${{ steps.setup.outputs.stack-root }}/indices
          ${{ steps.setup.outputs.stack-root }}/pantry
          ${{ steps.setup.outputs.stack-root }}/snapshots
        key: ${{ runner.os }}-stack-deps-${{ env.STACK_VERSION }}-${{ hashFiles('stack.yaml', 'stack.yaml.lock', 'package.yaml', '*.cabal') }}
        restore-keys: |
          ${{ runner.os }}-stack-deps-${{ env.STACK_VERSION }}-

    - name: Install dependencies
      if: (steps.stack-programs-cache.outputs.cache-hit != 'true' || steps.stack-deps-cache.outputs.cache-hit != 'true') && success()
      timeout-minutes: 15
      run: |
        stack build --test --no-run-tests --only-dependencies

    - name: Run tests with coverage
      timeout-minutes: 15
      run: |
        stack test --coverage --fast

    - name: Build
      timeout-minutes: 15
      run: |
        stack build --test --no-run-tests

    - name: Generate coverage report
      uses: 8c6794b6/hpc-codecov-action@v4
      with:
        target: stack:haskell-incentives-dsl-test
        out: ./codecov.json
        verbose: true

    - name: Send coverage report
      uses: codecov/codecov-action@v6
      with:
        files: ./codecov.json
        fail_ci_if_error: false # optional (default = false)
        verbose: true
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
